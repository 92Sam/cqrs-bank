enum ECreditLine{
    CREDIT_BASIC
}

type CreditLine implements CommonDateType {
    id: ID!
    name: String!
    description: String!
    currency: Currency!
    amount: Float!
    createdAt: String
    updatedAt: String
}

# DTOS Requests
input InputCreditLine {
    id: ID!
    name: String!
    description: String!
    currency: Currency!
    amount: Float!
}
